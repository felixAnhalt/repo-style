import type { Signal } from '../core/signals.js';
export async function scanASTTs(_root: string, _max?: number): Promise<Signal[]> {
  // TODO: Implement TS AST scan using TypeScript compiler API or ts-morph.
  return [];
}