import type { Signal } from '../core/signals.js';
export async function scanASTPython(_root: string, _max?: number): Promise<Signal[]> {
  // TODO: Implement AST walk (e.g., via tree-sitter or simple heuristics).
  return [];
}