import { describe, it, expect } from 'vitest';

// Placeholder: once the scanners are implemented, clone small public fixtures and assert determinism.

describe('scaffold', () => {
  it('loads', () => {
    expect(1 + 1).toBe(2);
  });
});